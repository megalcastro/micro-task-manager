# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build micro-task-manager`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t micro-task-manager`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system micro-task-manager && \
          adduser --system -G micro-task-manager micro-task-manager

COPY dist/apps/micro-task-manager micro-task-manager/
RUN chown -R micro-task-manager:micro-task-manager .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix micro-task-manager --omit=dev -f install

CMD [ "node", "micro-task-manager" ]
